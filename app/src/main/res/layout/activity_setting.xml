<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".mine.SettingActivity"
    android:background="@color/white_mode">

    <include layout="@layout/include_toolbar"/>

    <ImageView
        android:id="@+id/iv_logo_setting"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/icon_marshal_logo"
        app:layout_constraintTop_toBottomOf="@+id/toolbar_custom"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/tv_logo_name"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        app:layout_constraintTop_toBottomOf="@+id/iv_logo_setting"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:gravity="center"
        tools:text="marshalIM 0.02"
        android:textColor="@color/text_color_black"
        android:textSize="16dp"
        />


</androidx.constraintlayout.widget.ConstraintLayout>