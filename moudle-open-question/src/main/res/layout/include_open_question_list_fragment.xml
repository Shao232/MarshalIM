<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_mode_white"
    android:scrollbarThumbVertical="@color/im_chat_sender_bubble_color"
    android:scrollbars="vertical">

    <include layout="@layout/include_toolbar"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:scrollbarThumbVertical="@color/gray_line"
        android:scrollbarSize="10dp"
        android:scrollbars="vertical"
        android:background="@color/bg_mode_white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbar_custom" />

</androidx.constraintlayout.widget.ConstraintLayout>